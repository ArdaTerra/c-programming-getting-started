#include <stdio.h>
int main(void) 
{
    int num, grade, i;
    double avg;
    int sum = 0;
    scanf("%d",&num);
    for(i=0 ; i < num ; i++)
    {
        scanf("%d",&grade);
        sum = sum+grade;
    }
    avg = (double)sum;
    printf("%.2lf\n", avg/num);
    return 0;
}
