#include <stdio.h>

int main(void)
{
    int i, num, times;
    num = 8;
    times = 0;
    for(i=0; i<11; i++)
    {
        printf("%dx%d = %d\n", times, num, times*num);
        times = times + 1;
    }
    return 0;
}
