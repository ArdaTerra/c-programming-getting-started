Create a program that displays on the screen a square of n x n stars, 
with the integer n given as an input.

Hint 1 :

Use for loops with the user-entered integer as a boundary to produce the squares.

Examples
Input:
5
Output:
*****
*****
*****
*****
*****

Input:
3
Output:
***
***
***

#include <stdio.h>
int main(void)
{
    int n = 0;
    int i;
    scanf("%d", &n);
    for(i=0; i<5; i++)
    {
        printf("*****\n", i);
    }
    return 0;
}

/*second solution*/

#include <stdio.h>
int main() 
{
    int i, j;
    int n = 0;
    scanf("%d", &n);

    for(i = 0 ; i < n ; i++)
    {
        for(j = 0 ; j < n ; j++)
        {
            printf("*");
        }
        printf("\n");
    }
    return 0;
}
